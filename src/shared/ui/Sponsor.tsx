
const Sponsor = ({className}: {className?: string}) => {
  return (
    <div className={`w-38 h-8 flex-center mx-auto rounded-full text-center text-text-muted font-lato font-normal text-xs border border-border-line02 bg-bg-input ${className}`}>
          Powered by Schoolzy
    </div>
  )
}

export default Sponsor
